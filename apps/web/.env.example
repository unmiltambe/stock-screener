# Backend base URL the SPA calls.
# Local dev (backend in header mode, no token needed):
VITE_API_URL=http://localhost:8000
# Deployed AWS API (requires a Cognito token — auth wiring is the next step):
# VITE_API_URL=https://7x1e7unmh5.execute-api.us-east-1.amazonaws.com
