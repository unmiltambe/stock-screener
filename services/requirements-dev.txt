# Local dev/runtime dependencies for the backend.
# Use this when you don't want an editable install (works with any pip version):
#   pip install -r requirements-dev.txt
# Then run tests / the API from THIS directory (services/), so core, adapters,
# and api import via the current working directory.

fastapi>=0.110
uvicorn>=0.29
mangum>=0.17
yfinance>=0.2.40
boto3>=1.34
pytest>=8.0
httpx>=0.27
